export default function PlaceholderState({ title }) {
  return (
    <section className="container section-space">
      <div className="placeholder-state reveal">
        <h2>{title}</h2>
        <p>Page shell is ready. Content will be added in the next phase.</p>
      </div>
    </section>
  );
}
